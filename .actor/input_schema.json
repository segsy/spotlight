{
    "title": "Input schema of the spotlight Multi Scrapping System",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "sources": {
            "title": "Sources to Scrape",
            "type": "array",
            "description": "List of social media platforms or URLs to crawl",
            "items": {
                "type": "string"
            },
            "default": [
                "https://www.youtube.com/watch?v=WNS-n_O21TQ&t=4s",
                "https://www.instagram.com/explore/tags/javascript/"
            ]
        },
        "maxRequestsPerCrawl": {
            "title": "Max Requests per Crawl",
            "type": "integer",
            "description": "Maximum number of requests that can be made by this crawler.",
            "default": 100
        },
        "filters": {
            "title": "Filters",
            "type": "object",
            "description": "Sorting and time filters for platforms that support them",
            "properties": {
                "sort": {
                    "type": "string",
                    "default": "hot"
                },
                "timeRange": {
                    "type": "string",
                    "default": "week"
                }
            }
        }
    },
    "required": ["sources"]
}
